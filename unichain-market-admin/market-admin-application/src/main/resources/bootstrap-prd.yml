server:
  port: 11011
suixingpay:
  config:
    uris:
    - http://172.27.0.16:9000       # 配置中心服务地址，必须配置
    username: admin                # 调用接口用户名(非配置中心登录的用户名和密码)
    password: 123456
    profile: prd                  # 环境名称，默认值为spring.profiles.active的第1个值
